import express from 'express';

const router = express.Router();

router.use(express.json());

router
    .get('/:id', (req, res) => {

    })
    .put('/:id', (req, res) => {

    })
    .post('/', (req, res) => {

    });

export default router;
